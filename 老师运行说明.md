# 运行说明（老师专用 · 开箱即用）

> 本压缩包**已包含数据库、向量索引、LLM 密钥**，无需重新初始化，按下面 3 步即可运行。
>
> 系统：AI 赋能下的个性化学习路径系统（北科大 MBA · 人工智能课程作业 L-2）

---

## 一、环境要求

| 软件 | 版本 | 检查命令 |
|------|------|---------|
| Python | 3.11 ~ 3.13 | `python --version` |
| Node.js | 18+ | `node --version` |

> 若未安装：Python 从 https://www.python.org 下载（安装时勾选 **Add to PATH**）；Node.js 从 https://nodejs.org 下载 LTS 版。

---

## 二、安装依赖（首次运行一次）

在项目根目录打开 **PowerShell** 或 **终端**，依次执行：

```powershell
# 1) 创建并激活 Python 虚拟环境
python -m venv .venv
.venv\Scripts\Activate.ps1        # Windows PowerShell
# 或 .venv\Scripts\activate.bat   # Windows CMD
# 或 source .venv/bin/activate    # Mac/Linux

# 2) 安装后端依赖（用国内镜像更快）
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 3) 安装并构建前端
cd ymy
npm install
npm run build
cd ..
```

> 说明：`pip install` 会下载 bge-m3 等较大依赖，首次约 5–10 分钟。
> 前端 `npm run build` 后产物在 `ymy/dist/`，由后端自动托管。

---

## 三、启动运行

```powershell
# 确保虚拟环境已激活（命令行前缀有 (.venv)）
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

启动后**等待约 1–2 分钟**（首次加载 bge-m3 向量模型，日志出现 `RAG 预热完成` 即就绪），
然后浏览器打开：

### 👉 http://localhost:8000

---

## 四、使用流程（5 步闭环）

1. **画像录入** — 填昵称、学习目标（可选"AI 作业"）、认知水平、学习风格
2. **诊断测验** — 抽题作答（约 30–40 秒出诊断结果，多 Agent 在调用大模型）
3. **学习路径** — 看闯关路线图 / 知识图谱 / 路径质量评分
4. **学习工作台** — 标记完成、提交反馈让 AI 动态调整路径
5. **评估看板** — 掌握度分布、推理日志、路径明细

> 详细功能说明见 `docs/网页功能流程汇报.pdf`

---

## 五、常见问题

**Q：诊断很慢 / 第一次诊断卡住？**
首次启动 bge-m3 模型加载需 1–2 分钟，请等日志出现 `RAG 预热完成` 后再操作。
单次诊断约 30–40 秒（大模型多次调用），属正常。

**Q：诊断结果显示 "Mock 兜底"？**
说明大模型暂时不可用（网络 / 密钥 / 额度问题），系统自动降级到本地启发式算法，
闭环仍可完整演示，只是结果不是真实大模型输出。

**Q：PowerShell 提示"禁止运行脚本"无法激活 venv？**
以管理员身份运行 PowerShell 执行：
`Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser`

**Q：端口 8000 被占用？**
换端口启动：`python -m uvicorn app.main:app --port 8001`，浏览器访问 http://localhost:8001

---

## 六、技术栈

后端 FastAPI + LangGraph（诊断/规划/评价/优化 四 Agent）
RAG bge-m3 + Chroma 向量库 ｜ 大模型 火山方舟 Doubao
前端 Vue 3 + Element Plus + ECharts ｜ 数据库 SQLite
